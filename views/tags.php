<p>This action is performs a very specific function. It assumes you have the Tagger Module installed.
	It also assumes there is a comma-separate 'Raw Tags' field in your blogs.  At this time the field_ids
	are hard-coded.  Click the link if you understand what I'm sayin.</p>

<?
echo '<a href="'.$_base_url.AMP.'method=queryTags">Populate Tagger Fields</a>';

